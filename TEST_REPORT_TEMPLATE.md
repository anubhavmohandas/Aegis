# Aegis Test Report

Copy this for each test session. One template per bug/observation, not per day — file a new copy each time something doesn't match what you expected.

## Environment

- OS:
- OS build/version:
- Python version:
- Build type: `python main.py` (source) / PyInstaller / py2app
- Build tool version (if packaged):
- Git commit:

## Step

<!-- What you did -->

## Expected

<!-- What should have happened -->

## Actual

<!-- What actually happened -->

## Log / traceback

```
<paste full output here — don't trim, the missing line is usually the one that matters>
```

## Reproducible

Always / Sometimes / Once

## Notes

<!-- Anything that distinguishes this from a clean run — e.g. "only happens in the
     packaged build, works fine with `python main.py`" is the single most useful
     note you can add, since it immediately separates packaging bugs from logic bugs -->
