PyYAML>=6.0
watchdog>=4.0
plyer>=2.1
pystray>=0.19
Pillow>=10.0
psutil>=5.9
anthropic>=0.40
openai>=1.50
PySide6>=6.8   # only needed to run ui/timeline_app.py -- the background monitor (main.py) doesn't require it
